import { Context } from "grammy";

const START_MESSAGE = `👋 Добро пожаловать в Hisbbot!

Как пользоваться:
• Напишите сумму и категорию в одном сообщении.
• Примеры:
  — кофе 30 000
  — 50 000 такси
  — зарплата 5 000 000

Бот сам определит расход или доход и сохранит запись.

📊 Статистику и историю можно смотреть в дашборде.`;

export async function handleStart(ctx: Context) {
	await ctx.reply(START_MESSAGE);
}
